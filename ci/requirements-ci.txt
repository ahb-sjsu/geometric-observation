# Tier-A CI dependencies (CPU-only reproduction gate). No GPU/torch/transformers:
# Gate-B (GO-P-2026-020/021), GO-4 (Atlas /archive) and GO-5-ADC are excluded by
# design -- they require Atlas/GPU or external corpora and run as committed artifacts.
numpy>=1.26,<3
scikit-learn>=1.3
