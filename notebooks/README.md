# Notebooks — one-click replications (PROTOCOL §10)

| Notebook | Claim | Ledger | Runtime |
|---|---|---|---|
| [`GO7_one_click.ipynb`](GO7_one_click.ipynb) | description rate vs conditional reset work are separate resources of one stored code (Paper V, Thm 1) | GO-7 `[replicated]` | ~2 min, CPU, numpy+matplotlib only |

**Replication call.** Run it, change the seed, change the target channel. If you can
make the separation disappear while the gate preconditions hold, that is a
`[refuted]`-grade finding — open an issue. Attempts are logged either way; the absence
of counterexamples is only evidence if the search is visible (PROTOCOL §9).
