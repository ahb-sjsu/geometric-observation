# GO-P-2026-066 — GO-12 Theorem 1: the conditional-variance reduction and the access-class interpolation

Second GO-12 registration: settles the causal-prefix eraser in the
single-record setting (the open object flagged by Conjecture 2′ at
v0.3), R-IND-5-verified pre-seal with five sharpenings folded (scope of
the σ-algebra hypothesis, q<1, active regime, exact gap formula with
float-resolution flag, a∈(0,1) load-bearing; record-class optimality
imported from the static theorem's converse via the channel-agnostic
sufficiency step).

**The theorem under net.** For the AR(1) context family with
single-letter records: L_G(D) = ½log₂ g*(ρ², 1/(1−q_G), D) for every
context σ-algebra G generated by a nonempty subset of the observations
— the whole access class enters through q_G = Var(V_t|G) alone. The
nested classes slice ⊂ causal prefix ⊂ full path at equal lag Δ map to
single-sample, Kalman fixed-lag, and noncausal-Wiener variances, giving
the strict interpolation L_slice > L_prefix > L_path with the exact
prefix–path gap C^{2Δ}(P_f − q_path). The dynamic conditional-Landauer
curve W(Δ) = kT·ln2·L_prefix(D,Δ) falls geometrically from the
filtering cost to the smoothing floor. The slice case recovers 065's
(B) substitution exactly.

Governs `experiments/go12_prefix_family.py` (numpy+scipy, single run;
sentinel `===GO12PF-JSON===` with `===END===`; flag `GO12PF_supported`).

```yaml
id: GO-P-2026-066
date: 2026-08-05
retrospective: false
kind: theorem-verification (C3 numerical falsification of an R-IND-5-verified analytic result)
claim: "GO-12 Theorem 1: for single-letter records on the AR(1) context
  family, every observation-subset sigma-algebra enters the conditional
  coordinate through q_G = Var(V_t|G) alone -- L_G(D) = 1/2 log2
  g*(rho^2, 1/(1-q_G), D) -- and the slice/prefix/path access classes
  (single-sample, Kalman fixed-lag, noncausal-Wiener variances)
  strictly interpolate at every finite lag, with exact prefix-path gap
  C^{2 Delta}(P_f - q_path); the slice case reproduces 065's (B)
  substitution."
harness: experiments/go12_prefix_family.py   # GOVERNED seed 20260918; pilot seed 20260917, disclosed below
power: |
  Deterministic analytic-reproduction gates; per PROTOCOL 5.1 the bars
  carry pilot margins: s1 reduction bar 1e-6 vs pilot 2.2e-13 (~5e6x)
  over six access classes incl. asymmetric/straddle/gapped; s2 bars
  1e-12 / 1e-8 vs 0.0 / 8.9e-16; s3 bars 1e-6 / 1e-4 vs 1.7e-16 /
  1.9e-16; s4 strictness bars 1e-9 (slice-prefix) and 1e-12
  (prefix-path) vs pilot minima 0.047 and 3.3e-8 (the latter the lag-6
  geometric tail; float-underflow lags excluded by design, pole edges
  gated non-strictly at 1e-12 tolerance). Every margin >= 1.3x.
pilot: |
  ONE pilot, seed 20260917, full harness, 2.7 s: ALL PASS with drafted
  bars unchanged (zero bar recalibrations). Values: s1 worst gap
  2.2e-13; s2 0.0 / 8.9e-16; s3 Kalman 1.7e-16, Wiener 1.9e-16; s4 min
  gaps 0.0470 (slice-prefix) and 3.3e-8 (prefix-path). The R-IND-5
  pass (PASS; five wording sharpenings, no computational changes)
  completed after the pilot and before this seal; the harness is
  unchanged from the piloted version.
prediction:
  s1_reduction: |L_reduction - L_direct| <= 1e-6 on six access classes
    (slice, two-sample, straddle {-2,+3}, gapped {-5,+2,+7}, prefix
    window, full window) at (a,rho,tau2,D) = (0.8,0.7,0.4,0.3)
  s2_slice_065: q_slice matches 1 - a^{2D}/(1+tau2) to 1e-12 and s_eff
    matches (1+tau2)/a^{2Delta} to 1e-8
  s3_closed_forms: Riccati+fixed-lag recursion matches windowed
    covariance to 1e-6; Wiener integral matches to 1e-4
  s4_interpolation: q_slice > q_prefix > q_path strict over lags
    {0,1,3,6} at two instances with min gaps > 1e-9 / > 1e-12; pole
    edges a in {0.05, 0.99} ordered within 1e-12 tolerance
falsification: s1 failing refutes the conditional-variance reduction;
  s2 failing breaks consistency with sealed 065 (cross-contradiction
  to investigate); s3 failing refutes the closed-form identifications;
  s4 failing refutes strict interpolation. Instrument-vs-physics per
  PROTOCOL 5.1: SLSQP non-convergence is a logged instrumentation miss
  (dated-amendment rerun only).
design:
  stopping: fixed design, single governed run, seed 20260918, after
    the one disclosed pilot (seed 20260917); no further pilots or
    attempts under this ID
  runtime: ~3 s single-threaded (pilot: 2.7 s)
controls: [asymmetric/straddle/gapped access classes (s1), sealed-065
  cross-consistency (s2), two independent closed-form routes vs raw
  covariances (s3), pole-edge probes with the a=0 degeneracy excluded
  by hypothesis (s4)]
amendments: []
hash: sha256:4a9e1fe6e47dfbba64e1815bf20142a27e364c05b4038c59bed456262744e5b7
```

## Falsification

A pass upgrades GO-12 to Theorem 1 + Facts 1–2 netted, with Conjecture
2′ reduced to its process-rate residue. The verifier's provenance note
stands either way: record-class optimality is imported from the static
theorem (netted at 060/064), so an s1 fail here would indict the
reduction, not the static converse.
