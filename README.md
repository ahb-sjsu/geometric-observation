# Geometric Observation

[![CI](https://github.com/ahb-sjsu/geometric-observation/actions/workflows/ci.yml/badge.svg?branch=master)](https://github.com/ahb-sjsu/geometric-observation/actions/workflows/ci.yml)
[![Latest release](https://img.shields.io/github/v/release/ahb-sjsu/geometric-observation?label=release)](https://github.com/ahb-sjsu/geometric-observation/releases/latest)
[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.21776291.svg)](https://doi.org/10.5281/zenodo.21776291)
[![Last commit](https://img.shields.io/github/last-commit/ahb-sjsu/geometric-observation/master)](https://github.com/ahb-sjsu/geometric-observation/commits/master)
[![Sealed preregistrations](https://img.shields.io/badge/sealed_preregistrations-84-blue)](claims/REGISTRY-ACCOUNTING.md)
[![No file drawer](https://img.shields.io/badge/registry-85_IDs_%C2%B7_gap--free-blue)](claims/REGISTRY-ACCOUNTING.md)
[![Claims ledger](https://img.shields.io/badge/claims-LEDGER-blue)](claims/LEDGER.md)
[![License: MIT](https://img.shields.io/badge/license-MIT-green)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.12-3776AB)](ci/requirements-ci.txt)
[![Protocol](https://img.shields.io/badge/protocol-registration_precedes_measurement-8A2BE2)](PROTOCOL.md)

The fourteenth work in the geometric series, and its **evidence repository**. The book
synthesizes:

- **Paper I** — *Keep the Angle* ([`the-angular-observer`](https://github.com/ahb-sjsu/the-angular-observer)): the spectral/angular result.
- **Paper I.5** — *The Angular Observer as a Manifold Recognizer* ([`the-angular-observer/paper2`](https://github.com/ahb-sjsu/the-angular-observer/tree/main/paper2)): the five-instrument recognizer that names a manifold or certifies none — *dimension emerges before shape*.
- **Paper II** — the compression-as-observation work ([`turboquant-pro`](https://github.com/ahb-sjsu/turboquant-pro)): keys/values asymmetry, rank certificates, the (A2) probe, operator-regime tracing.
- **Paper III** — [**Observation Theory**](paper/observation-theory.pdf) ([source](paper/observation-theory.tex)): the information-theoretic synthesis — consumer-relative rate–distortion, the two-observer successive-refinement region ([`paper/two-observer-theorem.tex`](paper/two-observer-theorem.tex)), and the omission floor.
- **Paper IV** — [**The consumer-relative flip**](paper/paper-IV-consumer-relative-flip.pdf) ([source](paper/paper-IV-consumer-relative-flip.tex), [TMLR build](paper/paper-IV-tmlr.tex)): the empirical spine — at matched bits, a code that preserves what the *consumer* reads beats a reconstruction-optimal code on the downstream task *while reconstructing the signal worse*, demonstrated across domains and physics.
- **Paper V** — [**A Rate–Work–Distortion Region for Consumer-Relative Observation**](paper/consumer-relative-landauer.pdf) ([source](paper/consumer-relative-landauer.tex), [Overleaf zip](paper/consumer-relative-landauer-overleaf.zip)): the thermodynamic extension — a stored description carries **two separate resources**, the rate a consumer needs ($I(X;\hat X)$) and the ideal Landauer work its reset costs against retained side information ($I(X;\hat X\mid S)$); the full achievable region, the materialize-then-project barrier, multi-consumer coordinated reset, the Gaussian region with reset side information (scalar single-corner collapse with work discount $\to I(X;S)$; vector "reset water-filling" frontier), temperature-weighted water-filling, the staleness–work complement, and the semantic-invariance-of-erasure locality statement. Verified: **two** R-IND-5 fresh-context passes (0 errors total — ledger VI-8/VI-9; [revision notes](paper/consumer-relative-landauer-REVISION-NOTES.md)), theorem harnesses `verify_consumer_landauer.py` + `verify_gaussian_sideinfo.py` (GO-P-2026-042/044, both ALL PASS, CI-rerun), and three operational faces, each now `[replicated]` on two independent source families: **GO-7** (rate–work separation — 043 PASS 7/7, 045 multi-codebook PASS 6/6, 047 cross-source Gaussian PASS 6/6), **GO-8** (staleness: the reset threshold climbs with the age of the retained side information, tracking the predicted exchange — binary 049, Gaussian AR(1) 053), and **GO-9** (coordinated reset saves the shared-structure information — binary 050, Gaussian 054). ⚠ The GO-8/GO-9 Gaussian verdicts rest on a post-hoc bugfix to a control statistic, recorded in their ledger rows. Archived release: [doi:10.5281/zenodo.21776291](https://doi.org/10.5281/zenodo.21776291) (all versions; v1.2 = tag `paper-v-1.2`); one-click replication: [`notebooks/`](notebooks/).

- **Papers VI & VII** — *in preparation* (skeletons + full evidence package for venue work: [`paper/PAPER-VI-VII-EVIDENCE-INDEX.md`](paper/PAPER-VI-VII-EVIDENCE-INDEX.md)): **Paper VI**, the spectral conditional rate–distortion function and the aging context ([skeleton](paper/paper-VI-spectral-conditional.tex); source manuscript [GO-12](paper/go12-process-region.tex)) — staleness is access width, the conditional-variance reduction and the dynamic Landauer curve W(Δ), and the spectral pair (070/071) that four novelty sweeps and three library checks say was never written down; **Paper VII**, the dynamic complementarity tax ([skeleton](paper/paper-VII-dynamic-tax.tex); source manuscript [GO-13](paper/go13-dynamic-tax.tex)) — matrix-q universality, the tax-curve sign law, the binary twin (universality exactly Gaussian, approximately binary), the spectral m=2 theorem with two common prices, the m-record moment-convexity lemma (a candidate standalone communication), and a measured operational face (+0.090 vs +0.086 predicted). Every theorem: sealed prereg, governed harness, CI re-run, fresh-context adversarial verification.

into one claim: **compression succeeds for a consumer exactly when it preserves
what that consumer's functional distinguishes** — measured on the consumer's own
metric, never on reconstruction error.

## Observation Theory

Geometry, distortion, and reliability are properties of the **observation** — the
consumer's read operator, budget, and channel — not of the object observed. The
operative distortion is the reconstruction error read through the consumer,
`d_O = tr(P_C · Σ_δ)`; reconstruction (`tr Σ_δ`) is the corner `P_C = I`, where
Shannon rate–distortion and Gauss least-squares live. Paper III derives this,
proves the achievability + converse of a consumer-relative rate–distortion function
for a general source, gives the two-observer successive-refinement region, and
positions the framework as the identifiable geometric middle term between Shannon
and the information bottleneck. See
[`paper/observation-theory.pdf`](paper/observation-theory.pdf).

## House rule

> The book may not assert what the ledger cannot show.

Everything here is governed by [`PROTOCOL.md`](PROTOCOL.md): registration precedes
measurement, claims carry a class (`[proved]` / `[demonstrated]` / `[replicated]`
/ `[predicted]` / `[exploratory]` / `[refuted]`), and the umbrella principle
(Ch. 11) may cite only `[proved]`, `[replicated]`, and `[predicted]` rows.

## The falsifiable core

The framework is tautology-shaped unless specific claims are put at risk. Five
are (`PROTOCOL.md` §2): **GO-1** identifiability · **GO-2** quotient-(A2) transfer
· **GO-3** certificate vacuity · **GO-4** budget/wavelength inversion · **GO-5**
nuisance quotient. Each has a registry entry, a bar, and a falsification
condition. The Honest Negatives chapter carries every `[refuted]` row.

## Layout

| Path | What |
|---|---|
| [`PROTOCOL.md`](PROTOCOL.md) | Test protocol (governs every book claim) |
| [`prereg/`](prereg/) | Dated, hashed prediction-registry entries — written *before* the runs they govern ([`TEMPLATE.md`](prereg/TEMPLATE.md)) |
| [`claims/LEDGER.md`](claims/LEDGER.md) | One row per book claim; every table/figure resolves here |
| [`claims/REGISTRY-ACCOUNTING.md`](claims/REGISTRY-ACCOUNTING.md) | Every assigned prereg ID with a disposition — the no-file-drawer proof |
| [`DOMAIN-GENERALITY-SWEEP.md`](DOMAIN-GENERALITY-SWEEP.md) | The consumer-relative flip across domains, one row per sealed prereg + result |
| [`experiments/`](experiments/) | The three-arm instance runs (§3) and scripts |
| [`results/`](results/) | Sentinel-delimited result JSONs (committed, CI-rerun) |
| [`chapters/`](chapters/) | Chapter → claim map and drafts |

## Status — falsifiable core resolved; the flip is domain-general; the cost face extends to thermodynamics; the tax and its region are replicated; the dynamic theory is closed through the spectral pair and the m=2 endgame; causal erasure is netted through its convex program and an unconditional process limit; reference-coupled erasure is open

| Claim | Class | Evidence |
|---|---|---|
| **GO-1** identifiability | `[predicted]` ✅ | blind probe recovers the read subspace at 0.94 vs 0.06 chance; predicts the flip 12/12 |
| **GO-2** distortion (`tr P_C Σ_δ`) | `[demonstrated]` · `[replicated]` ✅ · Gate-B | recon-identical code 2.5× worse; the flip inverts with the consumer; holds on attention, retrieval, optimization, a trained Llama-3.2-3B layer (GO-021: reconstruction-invisible worse-arm call 16/16 at recon tied to 7.5e-9), and — via the sealed domain-generality battery — on acoustic and seismic arrays |
| **GO-3** certificate vacuity | `[demonstrated]` ✅ | derived EVT threshold locates retrieval death to ~5%, orders 14 corpora (ρ=0.99) |
| **GO-4** budget inversion | `[replicated]` ✅ | fixed-budget verdict inverts under budget-matched observation on real embedding manifolds |
| **GO-5** density quotient | `[refuted]` ❌ | 4 prospective misses; the density-nuisance mechanism is operator/spectral-confined (NEG-11) |
| **GO-6** output ≤ surrogate ≤ recon | `[demonstrated]` ✅ | at matched rate the three coders order on the consumer metric; the gap is the ker P_C entropy share |
| **GO-7** rate–work separation (Paper V) | `[replicated]` ✅ | the same stored code index decodes from reset side information at ~0.4·R̂, fails below its conditional content, fails absolutely without S — on **two independent source families** (binary two-bit 043/045, incl. 5 codebooks; scalar Gaussian 047 at the paper's §VI corner setting); 046 = logged instrumentation-window miss, physics gates 4/4 |
| **GO-8** staleness–work complement (Paper V) | `[replicated]` ⚠ | one fixed record, one fixed bin assignment: the decodable reset threshold climbs **0.10 → 0.55 bits/symbol** as the retained side information ages, tracking R_c − 1 + h₂(d̂ ∗ q_t) within one grid step at all eight ages; the same bin rate flips from 1% error (age 0) to 100% (age 32). Relevance lost to time is gained as erasure work. Held on **two source families** (binary Markov 049; Gaussian AR(1) 053, tracking the §VI discount). ⚠ The Gaussian verdict rests on a post-hoc control-statistic bugfix — see the ledger row |
| **GO-9** coordinated reset (Paper V) | `[replicated]` ⚠ | two consumer records sharing a component: resetting either one with the *other intact* lowers the threshold by the shared-structure information — **0.60 / 0.45 bits/symbol measured vs 0.476 predicted** — including on the record whose own reset side information is useless; mismatched pairing saves nothing. The operational face of TC(U₁; …; U_m | S); held on **two source families** (binary 050; Gaussian 054, 0.56× the asymptotic gap on both records). ⚠ Same caveat as GO-8 |
| **GO-10** complementarity tax ([note](paper/complementarity-tax.tex)) | `[replicated]` ✅ | one description serving two read operators pays a tax priced by the read-plane Gram determinant κ = det(BᵀΣ_xB), and **erasure pays it only against what the reset context doesn't already know**: the rate and work floors differ by exactly I(uᵀX, vᵀX; S). Rate side classical after reduction (Gray 1973; Xiao–Luo 2005 — attributed; the tax quantity and the entire work side found no prior). Theory R-IND-5-verified (VI-10, one error caught pre-assertion), C3 harness **ALL PASS 7/7** (055); operational face on **two independent source families**: Gaussian (058, PASS 6/6 — gap **0.266/0.094 bits**, 0.57×/0.37× of asymptotic) and binary (059, PASS 6/6 — gap **0.207/0.043 bits**, 0.77×/0.43× of the derived floor 1 − h₂(d̂ ∗ q)), shuffled-context nulls ≈ 0 on both, and on both the coordinated-split eraser tilts its budget to the S-opaque record — reset water-filling made operational. θ-sweep rate face deliberately unregistered (attributed theory, exactly verified by the 055 C3 harness; exploratory demo only) |
| **GO-11** the exact region behind the tax ([manuscript](paper/go11-conditional-region.tex)) | `[replicated]` ✅ | the conditional rate–work region solved where the tools reach: the **first vector-Gaussian common-reconstruction-type function** (closed form ½log₂ g\*, with g\* the larger root of Dsg² − (D+s−ρ²)g + (1−ρ²), s = 1+τ²), the m=1 frontier as a two-water-level system with the **misalignment theorem** — a strict rate–work tradeoff opens whenever the reset context is misaligned with the read (ρ² strictly between 0 and 1); Paper V's single-corner collapse is exactly the alignment degeneracy — and the m=2 region as a nine-parameter matrix program from which GO-10's tax-gap formula is *derived*. Eleven results — the **m=1 theory is closed and the m=2 frontier system settled** (manuscript v0.11): the exact CR function and frontier, Theorem 6's work-floor iff (attained iff the context is X-measurable-and-SDC or plane-blind — correcting the conjectured Σ_{Y\|S} to the encoder-accessible Σ_{Y\|V}), vector S solved by the generalized two-water-level system, higher-rank reads exactly characterized with the block-diagonal decomposition, interior-α uniqueness *proved* via matrix convexity, **Theorem 9: the two-water-level structure survives two consumers with 2×2 matrix water levels** (the noise-precision identity Σ_N⁻¹ = wM₀⁻¹ + (1−w)M₁⁻¹ + diag(μ) including its off-diagonal, per-mode 2×2 resolvents in any context dimension, μᵢ = 1/Dᵢ at the rate-only corner with no symmetry assumption), and **Theorem 10: the binary conditional CR function closed with an elementary complete proof** (for the DSBS(p) pair with context noise q: the symmetric family with tilt equation ℓ(d₀) − ℓ(d₁) = 2(1−2q)ℓ(u), discount face exactly 1 − h₂(u), anchored at Gray (q = 0) and the marginal RD function (q = ½), and retro-deriving 062's previously formula-less noisy-face discount as 1 − h₂(⅙) = 0.3500 vs the sealed 0.3432, the bias pairing with the explained face) — each R-IND-5-verified **before** assertion; C3 harnesses **ALL PASS** (060 for Thms 1–5, 063 for Thms 7–8 + uniqueness, 064 for Thms 9–10 with alphabet-2/4/6 optimality certificates), so **every theorem in the manuscript is harness-netted and re-derived by CI on every push**; and the **encoder-side tilt replicated on two source families** (061 Gaussian, PASS 6/6: advantage 0.595 b/sym, content at the Gray floor; 062 binary, PASS 6/6: advantage 0.5214 vs the BA-committed 0.5203 — agreement to 0.001 b/sym, with the pair-BA target landing exactly on binary Gray): a record tilted into the context's X-measurable direction carries a strictly larger context-decode discount at matched consumer distortion, while the analytically context-blind probes (aρ−b = 0; Y⊕V ⊥ V) show S-dependence 0.000 exactly on both families — records optimally *written in ink the eraser can read*, the encoder-side complement of 058/059's eraser-side tilt |
| **GO-12** the dynamic region, opened ([problem statement](paper/go12-process-region.tex)) | `[predicted]` ✅  | **staleness is access width, not delay** (opening control, 065, ALL PASS 4/4): with full context-path access, pure-delay aging is information-free — the recoding identity (Y, V, S^Δ) =d (Y, V, P^Δ S⁰) makes every σ(S)-measurable conditional functional exactly Δ-invariant (circulant-exact to 3.3×10⁻¹⁶; O(1/n) edge leakage, ratio 0.5000000000 under n-doubling) — while time-local slice access pays GO-11's static quadratic with the substitution set by the *encoder's* access: single-letter records at (ρ, s/a^{2Δ}), context-epoch-latent records at the strictly smaller (ρ·a^Δ, τ²), gap up to 0.053 bits, both strictly increasing in Δ toward ½log₂(1/D). GO-8's measured age-dependence is the slice regime's operational face. On record: the R-IND-5 pass *refuted* the draft's formula/scope pairing before seal; corrected, both pilots disclosed. **Theorem 1 (066, ALL PASS 4/4): the conditional-variance reduction** — every observation-subset σ-algebra enters the coordinate through q_G = Var(V_t\|G) alone, L_G = ½log₂ g*(ρ², 1/(1−q_G), D); slice/prefix/path access = single-sample, Kalman fixed-lag, and noncausal-Wiener variances, strictly interpolating at every finite lag (exact gap C^{2Δ}(P_f − q_path)) — the causal-prefix eraser settled in the single-record setting, and W(Δ) = kT·ln2·L_prefix is the exact dynamic conditional-Landauer curve, falling geometrically from the filtering cost to the smoothing floor as the eraser waits. Four novelty sweeps complete (the L-object is Gray's conditional RDF; the substitution device is Simeone–Permuter's; the dichotomies and the reduction are ours). **Theorem 2-spectral (070, ALL PASS 4/4): the spectral conditional rate-distortion function** — the theorem four sweeps and three library checks say was never written down: at the work endpoint, L(D) is the equal-slope allocation of per-frequency static quadratics, ∫ ½log₂ g*(ρ²(ω), s(ω), D(ω)/S_Y(ω)) dω/2π over mean-D allocations — the water level's *third promotion* (scalars → matrices → spectra) completed for the single-consumer face, with classical Kolmogorov–Pinsker reverse water-filling recovered exactly at τ²→∞ and the static theorem at flat spectra. Per-mode convexity proven (with the explicit saturation constant); cross-mode records gain nothing (verified to 1e−13 including the weighted case); the Toeplitz transfer to finite windows is honestly scoped as imported-with-four-lemmas, with clean O(1/n) numerical support. **The weighted spectral theorem (071, ALL PASS 3/3) closes Conjecture 1 at circulant scope**: J_w decomposes per-frequency at every w, each mode carrying the two-water-level system (γ₀(ω), γ₁(ω)) under *one* common distortion price — the conjectured "pair of prices" refuted as phrased and rescoped to (w, μ). The third promotion is complete on both faces of the region. Open: the Toeplitz-transfer lemmas, the process-rate causal object |
| **GO-13** the dynamic complementarity tax ([problem statement](paper/go13-dynamic-tax.tex)) | `[demonstrated]` ✅  | **Theorem 1 (067, ALL PASS 4/4): the matrix-q reduction** — the eraser's access enters the two-consumer coordinates only through Σ_{T\|G} = Σ_T − (1−q)·ccᵀ, so with scalar context every coordinate and both taxes are functions of q alone: **equal-q access classes pay identical taxes** (universality proven and gated at 6.7×10⁻¹⁵ across structurally different classes), coordinates nondecreasing in q, and CT_W → CT_R as access degrades completely. On record: the pre-seal verifier *refuted* the draft's monotone-tax clause — there are regimes where staleness *raises* the tax by 0.16 nats and regimes with a kink where the binding consumer switches. **Theorem 2 (068, ALL PASS 4/4): the tax-curve envelope sign law** — dCT_W/dq ∝ (joint context coupling in the M₁⁻¹ metric) − (binding consumer's coupling): staleness raises the tax exactly when the joint record couples to the context harder than the binding consumer does; the max-switch kink is always downward; w=1 exactly flat. The exploratory 18-point regime map: the tax rises at 16/18 points, decreases only in shallow slivers where the binding consumer is context-rich. **Operational face (069, rerun ALL PASS 6/6 after a one-cell V6 instrumentation amendment, physics 5/5 both runs): the measured tax rises +0.090 under staleness** (asymptotic prediction +0.086) at the regime-map instance frozen before any instrument code existed, **and two structurally different access classes tuned to equal q show equal thresholds and equal taxes to 0.001** — both theorems carry a measured face on materialized records. **Theorem 3 (072, ALL PASS 4/4): the binary twin** — slice staleness collapses exactly (q_eff = q∗δ_Δ), every finite access class enters through the posterior reliability distribution with a generalized tilt equation, and single-q universality fails *exactly* (gap ~9×10⁻⁵ at 50-digit precision) while holding *approximately* (≤2.1×10⁻⁴): universality is exactly a Gaussian privilege, approximately a binary one. **Theorem 4 (073): the spectral m=2 theorem** — per-frequency matrix water levels under two common per-consumer prices (where the “pair of Lagrange prices” genuinely lives; static kernel attributed to Xiao–Luo and Stylianou et al.). **Theorem 5 (074): the m-record moment-convexity lemma, PROVED** — G = −log det(I−Z) with Z jointly matrix-convex: the weighted m=2 program is convex in moment coordinates, Theorem 9’s uniqueness flag is resolved, and the spectral two-price allocation is globally optimal. The static m=2 theory is closed. Open: the regime map beyond the exploratory grid, the operational dynamic-tax faces at m=2 |
| **GO-14** causal erasure — the process-rate coordinate ([statement v0.2](paper/go14-causal-erasure.tex)) | `[predicted]` ✅ | **Theorem 1 (076, ALL PASS 12/12): the Δ-lagged causally-conditioned coordinate settled** — definition (a) adopted at the probe fork ((b) floors 0.0136 bits above block and breaks the chain rule; (c) collapses to the path endpoint); the **exact chain rule** n·L_a = I(T^n;Ŷ^n\|S^n) + C_Δ with the smoothing-leakage charge C_Δ = 0 iff the record is Δ-lag causally simulatable; **collapse is a feasibility face** — N-only records collapse exactly while carrying 1.74 bits/cell, and below D = ρ²(n−Δ−1)/n the budget forbids every collapsing record (at the bound, one is exhibited to 1e-7); the **class-conditional sandwich** block_n < min L_a(Δ) ≤ diag-class < slice(Δ) is strictly *decreasing* in Δ (the v0.1 conjecture's sign error, caught and fixed), closing at rate λ_s^{2Δ}, and the diagonal class is *provably beaten* for L_a (failed first-order certificate + an explicit 2.4×10⁻³-bit feasible non-diagonal improvement — every quoted minimum is an upper bound by construction); the **smoothing pole is cell-local-scoped**: λ_s = a(1−K), per-cell leak ratios → λ_s⁻² = 7.955 (relerr 1.7×10⁻⁴) with the (n−Δ−1)/(n−Δ−2) aggregate prefactor, while code-mixing records decay measurably slower (rwf ≈ 0.255/lag ≈ λ_s²/ρ² empirically — open); and **L_a is not bookkeeping** — the n-pinned gap static(q_path) − block_n (0.0213/0.0263 at n=8/16) converges to the spectral gain 0.0313, block_∞ = 0.52995 by direct per-frequency Lagrangian allocation (the pilot exposed and replaced a wrong closed-form generalization — on record in the prereg). **Process-limit face (078, ALL PASS 19/19)**: the limit exists at O(1/n); the U-independence hypothesis is load-bearing (chain rule exact iff Δ-lag-causal U-coupling; outside the family the identity fails and the sandwich inverts — counter-values on record); the optimizer mechanism is horizon-matched V-cancellation (sign boundary exactly at lag Δ); per-lag ratios rise toward λ_s⁻² from below with c_diag ∈ [0.105, 0.125]; no tested causal-conditioning spectrum matches. **Theorems R+C (079, ALL PASS 14/14): the family program is CONVEX** — every KKT point global, the 078 anchors certified two-sided, the sandwich margin a computed interval, the no-spectrum gaps certificate-strict at every computed n; U-independence one-premise-two-legs; the diagonal class a convex linear section. **Transfer face (080, ALL PASS 23/23): the process limit exists unconditionally** (subadditivity + Fekete; the lower transfer side is a theorem), the proposed κ-per-side lemma is **refuted** with D-feasible witnesses reaching ~19κ (the refutation is gated), and the reverse direction, the c/n bound and the Δ=0 plateau stand openly conditional on numbered Hypothesis (H*) — whose no-slack character is gated too (base n=24 fails by 4.7e-5); the diag ladder is certified within class. **Plateau face (082, ALL PASS 30/30): the process limit is bounded below by a certified stationary value at three lags** — a collapse identity plus periodization plus shift-averaging plus a box-free per-frequency certificate, needing no optimality, no convexity of the process rate and no differentiability; margins +0.01478/+0.00406/+0.00080 retire five hypotheses and the boundary charge, at two lags the transfer route could not reach. The chain is one-directional, so the limit is bracketed, not determined. **(R1) is now PROVED (083): the plateau chain at three lags is UNCONDITIONAL** — no (H*), (H**), (H***), (D), (F), no boundary charge, no (R1), no (R2); *unconditional* attaches to the chain, never to the Ψ value, and the chain stays one-directional so the limit is bracketed, not determined. **Achievability (084): the bracket narrows 51-fold unconditionally** — L^inf in [0.5627264963, 0.5627656412] at Δ=0 and correspondingly at Δ=1,2, from an explicit exactly-feasible record, depending on no lemma; Lemma W reduces the converse to a single density residual, measured at the noise floor. **FIR density (085): the equality, scoped** — the density step is the Hilbert-space projection fact, and Theorem D approximates an arbitrary near-optimal *feasible* record rather than the fixed point, so the circularity is removed rather than hidden; Lemma M seals *restated*, its window-independence and no-threshold claims withdrawn (only C(L,n) = o(n) is used, so the conclusion is untouched). Hence L^inf(Δ) = Ψ(D;Δ) at Δ = 0,1,2 **as a scoped theorem with five named inputs** — it identifies two objects and is *not* a licence to quote a value as exact: Ψ remains a two-sided certified bracket, the bracket corollary still governs the quotable number, and *unconditional* attaches to neither. **Per-cell convexity (086): refuted after the first cell** — the individual per-cell terms are not convex (pinned Jensen witness, +6.95e-02 bits the wrong way, three routes and exact rational arithmetic), while cell 0 is convex because it collapses exactly to the block bracket in scalar form. Theorem C is untouched and the same witness pair gates it: the total stays convex, the neighbours absorbing the concave cell — the regrouping is not a convenience, it is the proof. Open: minimizer uniqueness, the reset protocol, the rwf pole. Novelty gate CLEARED (five must-cites; arXiv + DBLP/Crossref passes on record) |
| **GO-15** reference-coupled erasure — the lookahead discount ([statement v0.1](paper/go15-reference-coupled.tex)) | `[exploratory]` | **Opened 2026-08-06** from GO-14's own verification pass: records permitted to correlate with the *noise* of the eraser's aging reference erase far cheaper (probe: 0.079 vs 0.567 bits/symbol at Δ=0, n=16 — an 86% collapse, sandwich inverted). The probe's operational verdict frames the campaign: the discount is gated by **encoder lookahead past the eraser's access horizon** (within-horizon reading buys ~nothing; full reference lookahead captures 57%; the rest needs resolving S into V+U — fictitious), and the mechanism is *reference-noise anticipation* (63–93% of coupling mass, negative, on the single first-beyond-horizon band). Statement v0.1 carries one proposition and four conjectures; novelty flank clean with its citation spine. Every number is a class-conditional upper bound carrying its noise floor — **no lower-bound face exists yet**, and building one (extending Theorem R to extended moments) is the campaign gate |
| **KV serving operational face** (077, [prereg](prereg/GO-P-2026-077-kv-consumer-relative.md)) | P1 `[demonstrated]` ✅ · P2 `[refuted]` | quality tracks measured u, not scorer width (+0.44 at 5% keep, bar 0.30; snapshot survives 97% eviction; wide window ≈ random eviction); the novel equal-u control refuted its own equality prediction on the pre-registered branch (+0.359 ± 0.068 at equal u, 5.3 SE) — equal scalar uncertainty is insufficient, exactly GO-13 Thm 1's r≥2 scoping in the wild. Arc: 056 miss → 075 burned → 077 split verdict |

**The flip is a property of observation, not of a regime.** The sealed cross-domain
sweep ([`DOMAIN-GENERALITY-SWEEP.md`](DOMAIN-GENERALITY-SWEEP.md)) carries the
consumer-relative flip — at matched bits, read-preserving beats reconstruction-optimal
downstream while reconstructing worse — across **≥5 domains and ≥3 distinct physics**:
synthetic ULA (031), LLM attention keys (021), acoustic direction-of-arrival
(LOCATA 033, AV16.3 034·D2), and seismic backazimuth (PDAR 034·D3), plus the
non-physical domains of legal-citation retrieval (035→036→039) and sperm-whale coda
dialect (038). Radar (034·D1) is a data-limited partial; gradient/curvature
optimization (034·D4) is an honest negative that *bounds* the flip to consumers whose
read operator is misaligned with signal energy — no file drawer.

Four faces of Observation Theory stand; the fifth is an honest negative. The COST
face now extends to thermodynamics: Paper V's rate–work–distortion region separates
what a consumer must receive from what a reset mechanism must irreversibly discard,
with two 0-error fresh-context verification passes (VI-8/VI-9) and the GO-7
operational demonstration behind it — and now culminates in the replicated
complementarity tax (GO-10) and its exact conditional region (GO-11), whose
mechanism is measured from both sides of the memory on two source families each:
the eraser allocates its budget to what its context cannot resolve (058/059), and
the encoder writes its record into what the context can (061/062); the theory now
runs through the m=2 frontier system and the exact binary CR function (Thms 9–10,
netted by 064), and the dynamic extension is open with its first control settled
(GO-12, 065: staleness is access width, not delay; 066: the
conditional-variance reduction settles the causal-prefix eraser).
GO-13 (the dynamic
complementarity tax) is open with its Theorem 1 netted (067). GO-14 (causal
erasure — the process-rate coordinate) is netted across four registrations: the
exact chain rule and the class-conditional sandwich (076), the process-limit face
with its horizon-matched mechanism (078), **Theorems R and C — the family
optimization is a convex program, so every KKT point is the global minimum and
the anchors are certified two-sided** (079), and the transfer face (080), where
subadditivity plus Fekete give the **process limit unconditionally** while the
proposed κ-per-side lemma is *refuted* and the remaining transfer results stand
openly conditional on a numbered hypothesis. GO-15 (reference-coupled erasure)
is open at `[exploratory]`: its opening probe found an 86% erasure discount
gated by encoder lookahead past the eraser's horizon, with no lower-bound face
yet — building one is that campaign's gate.
Campaign
archive: tags `go10-go11-1.2`, `go12-go13-1.0/1.1/1.2`, `go12-go14-1.0` (through the GO-14 process-limit face + the 077 split verdict), and `go12-go14-1.1` (the plateau chain unconditional, the 51.5x bracket narrowing, and the scoped equality at three lags) under the same concept
DOI; Papers VI/VII skeletons and the evidence-package index are staged for venue
work. **84 sealed
preregistrations** (040 void, 046/048/051/052 logged instrumentation misses, each
superseded and rerun; 069's V6 a one-cell exact-binomial instrumentation miss,
amended and rerun with physics 5/5 both times; 056 a registered honest miss at its effect floors → NEG-16;
075 an ID burned honestly — a KV-serving design failure caught by its own
disclosed pilot before any seal or governed run; 079's harness carried in the
artifact-self-consistency tier because its certified-width gates race a
BLAS-dependent solver stopping point, four disclosed revisions on record),
every one timestamped before its measurement; standing negatives **NEG-1…16** —
NEG-13 (the omission floor) resolved to `[demonstrated]` downstream on a trained
model (GO-027), and **NEG-15** records the Bell boundary: retrieval geometry does
not weaken Bell ([notes](experiments/HUBNESS-BELL-NOTES.md)). Every claim
resolves to a row in [`claims/LEDGER.md`](claims/LEDGER.md).
