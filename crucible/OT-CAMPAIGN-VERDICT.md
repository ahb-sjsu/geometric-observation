# The Crucible verdict — Observation Theory v1.0 is NOT declared

**2026-08-15. All seven tests resolved; the graduation rule of
`OT-V0.1-FREEZE.md` is applied exactly as frozen, before any test ran.**

## Scorecard

| test | claim | verdict |
|---|---|---|
| OT-7 | transformation & invariance laws | ✅ PASS (3 instrument seals) |
| OT-3 | identification lower bound; side-info cliff at d−k₀ | ✅ PASS (theorem + 6 cells) |
| OT-1 | disagreement follows read-subspace geometry | ✅ PASS (cos²θ to 4 decimals; 45° flip; 8/8 real heads, no refit) |
| OT-2 | loading is a covariance, not a distance | ✅ PASS (orthogonal shift: machine zero) |
| OT-5 | metric degrades monotonically toward selection | ❌ **REFUTED** (it's a step: right, then silent — never wrong-while-differential) |
| OT-4 | drift predicts + intervention fixes a real degradation | ❌ **REFUTED** (the real collapse is feedback compounding of a constant error; orientation does nothing; intervention correctly never ran) |
| OT-6 | laws transfer outside compression, zero modification | ✅ PASS (11.8×/3.6×; 400/400 per-query signs) |

## The rule, applied

- **G1** — ≥4 of OT-1…OT-5: **FAIL** (OT-1, OT-2, OT-3 = three).
- **G2** — OT-6 necessary: PASS.
- **G3** — mechanism-grade result: PASS (OT-3's theorem + surviving
  side-information prediction; OT-4's intervention never got its turn).
- **G4** — frozen-hash: PASS (`git diff a490e4e..HEAD -- PRINCIPLES.md`
  = the permitted banner only).

G1 fails, so **v1.0 is not declared**. No partial graduation exists.
The campaign closes with the verdict the freeze document wrote in
advance for this branch: as of this campaign, the five principles are
**five-sevenths a predictive theory** — three principles produced
predictions that survived attempts to kill them, including a theorem,
a zero-parameter law carried to real consumers, and a cross-domain
transfer at 400/400 — and the two principles about *where the theory
stops and when it moves* (P5's boundary shape, P4's drift-as-mechanism)
made owed predictions that measurement refuted.

## What closure unlocks (and what it does not)

Per the freeze, principle text may now be revised — **after** this
verdict, diffed against the frozen commit, and any revision re-enters
as v0.2 with its own owed predictions:

1. **P5 revision candidate** (from OT-5): the boundary is a *floor
   law* — the quadratic form holds at ceiling while any differential
   signal exists and fails closed (silence) at the response floor.
2. **P4 revision candidate** (from OT-4): drift is real and priced
   (C-11c, OT-7) but is not entitled to explain production
   degradations by default; its owed prediction must name a system
   where staleness, not feedback, is the dominant channel — teacher
   forcing is the discriminator that must be predicted to *pass*, not
   assumed.

What stands regardless of graduation, because it was earned inside
sealed bars: the transformation laws and invariance taxonomy (OT-7),
the confinement theorem and the side-information cliff (OT-3), the
cos²θ/45° geometry of consumer disagreement with its 8/8 real-head
transfer (OT-1), the change-of-measure loading law (OT-2), the
embedding-ranking transfer (OT-6), the step-shaped regime boundary
(OT-5's finding), and the mechanism verdict on the TurboQuant collapse
(OT-4's finding, plus the errata it forced upstream).

Side products of the campaign, filed where they belong: the
turboquant-pro codebook-identity errata (commit `c944052` there); a
readscope issue owed on the applicability gate's single-point
heuristic; four instrumentation lessons now written into the notes
(bar the parent not the quantized flip rate; condition recovery bars
on the target being hidden; manipulation-check every knob; verify the
consumer realizes the operator the prediction is about).
