# Observation Theory v0.2 — the post-closure revision record

**2026-08-15, after `OT-CAMPAIGN-VERDICT.md`. The freeze rules permit
revision only after the campaign verdict, diffed against the frozen
commit, re-entering with fresh owed predictions. This is that record.
Canonical text: readscope `PRINCIPLES.md` (v0.2); frozen v0.1 remains
immutable at readscope commit `a490e4e`.**

## What changed, exactly

**P1, P2, P3 — statements unchanged.** Their v0.1 owed predictions
were consumed by OT-1, OT-2, OT-3 (all survived; OT-3's became a
theorem) and now stand as instances. Each carries a new owed
prediction (below).

**P4 — revised.** Frozen v0.1 core: *"`P_C` is a process, not a
constant … acting on a stale operator has a computable cost"* with the
owed prediction that drift explains turboquant-pro's long-generation
degradation. OT-4 refuted the owed prediction decisively (teacher
forcing removes the consistent growth, sign p = 0.42; orientation does
nothing, B1 −0.020; the collapse is feedback compounding of a constant
~4.3-nat symmetric-codebook error). v0.2 keeps the process claim and
its measurements, and **adds the dominance obligation**: drift may be
named as a degradation's mechanism only with a feedback-severed
control the principle *predicts in advance it will pass*. The new
forbids-clause makes OT-4's failure mode (assuming the discriminator
instead of predicting it) a violation of the principle itself.

**P5 — revised.** Frozen v0.1 core: damage-prediction quality
*"degrades monotonically"* toward the selection regime. OT-5 refuted
the shape (Spearman 0.54 vs 0.80): measured behavior is a **step** —
ceiling accuracy down to differential fraction 0.094, then silence,
never wrong-while-differential (V4 held). v0.2 states the **floor
law**: full fidelity wherever any differential signal exists; failure
closed (silence) at the response floor. A new forbids-clause bans
*discounting* the metric in low-but-nonzero differential regimes,
which the old monotone reading would have wrongly licensed.

**No sixth principle.** The admission rule held through the campaign
and holds through the revision.

## The five new owed predictions (v0.2's live exposure)

| principle | new owed prediction | first substrate |
|---|---|---|
| P1 | ensemble codec preference from weight-averaged component traces, no ensemble probe | multi-head attention |
| P2 | forward transfer: real-measure operator predicted from synthetic-measure probe + alignment functional | real heads vs Gaussian probes |
| P3 | the noisy cliff: noise floors accuracy at a derivable level, never moves the cliff | theorem extension + synthetic |
| P4 | feedback-free staleness: streaming-retrieval damage grows with measured drift and is removed at derived cadence; the severing control passes trivially | OT-6 embeddings, drift by strata |
| P5 | the two floor curves: informative fraction decays with output quantization while informative-pair accuracy stays at ceiling | quantized-output consumer family |

None has been run. A v0.2 campaign (test set, graduation rule, freeze
of this text by hash) is **not** hereby declared — that is a separate
decision with its own seal, and the v0.1 precedent says it should not
be rushed: bars written in the afternoon failed by evening, and the
program was better for it.

## Continuity ledger

Kept from v0.1 with full force: the object definition; the OT-7
transformation laws and invariance taxonomy; the OT-3 theorem and
cliff; the OT-1 geometry with its real-head transfer; the OT-2 law;
the OT-6 cross-domain result; the OT-5 step and OT-4 mechanism verdict
as *findings*; and the instrumentation rules earned across ten seals
(three of which failed and are kept as executed).
