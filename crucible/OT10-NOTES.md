# OT-10 notes — FAIL as sealed, by an authoring defect the record already refuted

**Verdict: FAIL** (N3's cliff-edge-contrast sub-bar; the appendix
declared bars-never-adjusted, and that clause binds even when the bar
is the casualty).

What the run measured, all of it consistent with the theorem:

- **N1 PASS** — the derived floor `σ²(d−1)/λ₁²` matched the measured
  median error within **5%** at every σ across two orders of
  magnitude (e.g., 3.10e-5 predicted, 3.11e-5 measured at σ=1e-3),
  far inside the sealed factor-2 band.
- **N2 PASS** — log-log slope **2.01** against the predicted 2.
- **N3's meaningful half passed** — zero recoveries of a genuinely
  hidden component below the cliff at every noise level: the location
  is σ-independent exactly as Statement 1 proves.
- **N3's contrast sub-bar failed because it was wrong when written:**
  it demanded a ≥0.5 median-*affinity* jump across the cliff edge, but
  median affinity at k = d−1 is ≈ 1 − median Beta(½,(d−1)/2) ≈ 0.986 —
  a fact sitting in OT-3's committed v3 results (0.978 at k = m−1)
  when the bar was sealed. The cliff is in *exact identification*;
  affinity crosses it by ~0.02, not 0.5. The bar contradicted the
  program's own record and the author did not check.

**What may and may not be concluded:** the noisy-cliff *theory* —
Statement 1 (location) and Statement 2 (floor) — is empirically
supported on every quantity it actually predicts, at first-order
precision better than the sealed tolerance. The *test* is dead by its
own rules, and stays dead: for-the-record campaigns do not resurrect
tests by re-sealing bars that failed, whatever the reason. P3 keeps
the theorem file and these measurements as `[exploratory]`-grade
support; a future registration may cite the derivation but must write
its bars against the record.

Instrumentation lesson (the sixth of the campaign, and the most
embarrassing): **check every sealed bar against the program's own
committed measurements before sealing** — OT-3's numbers were one
file away.
